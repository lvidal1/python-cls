# .coveragerc to control coverage.py
[run]
omit =
    # omit anything in a .local directory anywhere
    */.local/*
    # omit everything in /usr
    /usr/*
    # Django defaults 
    */migrations/*
    */settings.py
